export enum AppRoutes {
    Catalog = '/',
    Quest= 'quest',
    Booking = 'booking',
    Reservation = 'reservation',
    Login = 'login',
    Logout = 'logout',
    Contacts = 'Контакты',
}

export enum FetchRoutes {
  Catalog = '/quest'
}
