export enum AppRoutes {
    Catalog = '/',
    Quest= '/quest',
    Booking = '/booking',
    Reservation = '/reservation',
    Login = '/login',
    Logout = '/logout',
    Contacts = '/contacts',
}

export enum FetchRoutes {
  Catalog = '/quest',
  Login = '/login',
  Logout = '/logout',
  Booking = '/booking',
  Reservation = '/reservation',
}
